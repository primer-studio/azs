{{-- TODO[check-view]: these codes are temporary to test, re-write UI with proper codes, set validation errors (check controller validate too), set old data, ... --}}

there is an problem:

<pre>
    log_id: <code>{{ $log_id }}</code> <br>
    message: <code>{{ $report_message }}</code> <br>
    exception_message: <code>{{ $exception_message }}</code> <br>
    request_uri: <code>{{ $request_uri }}</code>
</pre>
